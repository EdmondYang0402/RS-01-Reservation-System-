<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rs01.hotel.mapper.RoomTypeMapper">
    <sql id="roomTypeColumns">
        id, hotel_id, name, description, capacity, bed_type, base_price,
        total_rooms, status, create_time, update_time
    </sql>
    <select id="selectById" resultType="com.rs01.hotel.entity.RoomType">
        SELECT <include refid="roomTypeColumns"/> FROM room_type WHERE id = #{id}
    </select>
    <select id="selectByHotelId" resultType="com.rs01.hotel.entity.RoomType">
        SELECT <include refid="roomTypeColumns"/>
        FROM room_type WHERE hotel_id = #{hotelId} ORDER BY id
    </select>
    <select id="selectAllEnabled" resultType="com.rs01.hotel.entity.RoomType">
        SELECT <include refid="roomTypeColumns"/>
        FROM room_type WHERE status = 1 ORDER BY id
    </select>
    <update id="update">
        UPDATE room_type SET hotel_id=#{hotelId}, name=#{name}, description=#{description}, capacity=#{capacity},
        bed_type=#{bedType}, base_price=#{basePrice}, total_rooms=#{totalRooms}, status=#{status} WHERE id=#{id}
    </update>
    <delete id="deleteById">DELETE FROM room_type WHERE id = #{id}</delete>
    <select id="listByHotelId" resultType="com.rs01.hotel.entity.RoomType">
        SELECT <include refid="roomTypeColumns"/>
        FROM room_type WHERE hotel_id=#{hotelId} ORDER BY id LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
