<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rs01.hotel.mapper.HotelMapper">
    <sql id="hotelColumns">
        id, name, address, phone, check_in_time, check_out_time, status, create_time, update_time
    </sql>
    <select id="selectById" resultType="com.rs01.hotel.entity.Hotel">
        SELECT <include refid="hotelColumns"/> FROM hotel WHERE id = #{id}
    </select>
    <select id="selectAll" resultType="com.rs01.hotel.entity.Hotel">
        SELECT <include refid="hotelColumns"/> FROM hotel ORDER BY id
    </select>
    <update id="update">
        UPDATE hotel SET name=#{name}, address=#{address}, phone=#{phone}, check_in_time=#{checkInTime},
        check_out_time=#{checkOutTime}, status=#{status} WHERE id=#{id}
    </update>
    <delete id="deleteById">DELETE FROM hotel WHERE id = #{id}</delete>
    <select id="list" resultType="com.rs01.hotel.entity.Hotel">
        SELECT <include refid="hotelColumns"/> FROM hotel ORDER BY id LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
