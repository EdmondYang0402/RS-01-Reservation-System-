<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rs01.inventory.mapper.DailyInventoryMapper">
    <select id="selectById" resultType="com.rs01.inventory.entity.DailyInventory">SELECT * FROM daily_inventory WHERE id=#{id}</select>
    <select id="selectByRoomTypeIdAndStayDate" resultType="com.rs01.inventory.entity.DailyInventory">
        SELECT * FROM daily_inventory WHERE room_type_id=#{roomTypeId} AND stay_date=#{stayDate}
    </select>
    <select id="selectByRoomTypeIdAndDateRange" resultType="com.rs01.inventory.entity.DailyInventory">
        SELECT * FROM daily_inventory WHERE room_type_id=#{roomTypeId}
        AND stay_date &gt;= #{startDate} AND stay_date &lt; #{endDate} ORDER BY stay_date
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO daily_inventory (room_type_id, stay_date, total_inventory, reserved_count, out_of_service_count)
        VALUES (#{roomTypeId}, #{stayDate}, #{totalInventory}, #{reservedCount}, #{outOfServiceCount})
    </insert>
    <update id="update">
        UPDATE daily_inventory SET room_type_id=#{roomTypeId}, stay_date=#{stayDate},
        total_inventory=#{totalInventory}, reserved_count=#{reservedCount}, out_of_service_count=#{outOfServiceCount}
        WHERE id=#{id}
    </update>
</mapper>
